# Anthropic API key for AI validation and scanned-PDF vision extraction.
# Optional for local dev — without it, the app uses regex-only extraction.
# Get a key at https://console.anthropic.com/
ANTHROPIC_API_KEY=

# Server port (default: 3100). Set automatically on most hosting platforms.
# PORT=3100
